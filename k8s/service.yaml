apiVersion: v1
kind: Service
metadata:
  name: blend-service
spec:
  selector:
    app: blend-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30001
  type: NodePort
